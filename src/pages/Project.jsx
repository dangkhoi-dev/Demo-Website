import React from 'react';
import Project from '../components/Project';

const ProjectPage = () => {
    return (
        <main>
            <Project />
        </main>
    );
};

export default ProjectPage;
